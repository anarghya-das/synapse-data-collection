# Variant: same as published, but bad channels are handled by 'keep_all' instead
# of being dropped. Lets models compare channel-handling strategies on a fixed cohort.
defaults:
  - published
  - _self_
name: keep_all
channel_strategy: keep_all
