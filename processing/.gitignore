# Raw recordings — large, not version-controlled here
data/

# Generated outputs — ALL of them live in the data tree ($SYNAPSE_DATA_BASE,
# e.g. ub-polar:/data1/anarghya/synapse-data/outputs), never in git. Locations
# are defined in conf/*.yaml `paths:`; see outputs/README.md in the data tree.
outputs/

# Python / OS
__pycache__/
*.pyc
.DS_Store
*.log
